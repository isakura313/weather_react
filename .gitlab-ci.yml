
pages: # the job must be named pages
  image: node:latest
  stage: deploy
  script:
    - npm i pnpm -g
    - pnpm i
    - pnpm run build
    - cp -a dist/. public/
  artifacts:
    paths:
      - public # artifact path must be /public for GitLab Pages to pick it up
  only:
    - main